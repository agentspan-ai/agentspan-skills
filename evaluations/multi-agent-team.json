{
  "name": "multi-agent-team",
  "description": "Create a sequential pipeline with >> operator",
  "query": "Build a research team with a researcher agent and a writer agent that work in sequence.",
  "expected_behavior": [
    "Create two Agent instances with distinct names and instructions",
    "Use >> operator to create sequential pipeline",
    "Run the pipeline with rt.run()",
    "Show results from both agents"
  ],
  "success_criteria": [
    "Uses >> operator for sequential pipeline",
    "Each agent has a unique name",
    "Pipeline is run with rt.run(pipeline, prompt)",
    "Accesses sub_results for individual agent outputs"
  ]
}
